# Copy to .env. Leave GEMINI_API_KEY blank to run entirely offline on the
# deterministic fallback engine (app/services/fallback_engine.py) -- useful
# for demos/CI without API costs. Set it to drive the swarm with real Gemini.
GEMINI_API_KEY=
GEMINI_MODEL=gemini-2.0-flash
LLM_PROVIDER=auto
LOCAL_LLM_ENABLED=true
LOCAL_LLM_PROVIDER=ollama
LOCAL_LLM_MODEL=
LOCAL_LLM_BASE_URL=http://localhost:11434
GROK_ENABLED=false
GROK_API_KEY=
GROK_MODEL=
GROK_BASE_URL=https://api.x.ai/v1
LLM_CLOUD_FALLBACK=true
LLM_CONFIDENCE_THRESHOLD=0.80
RAG_ENABLED=true
RAG_TOP_K=6
RAG_MAX_CONTEXT_TOKENS=3500
EMBEDDING_MODEL=all-MiniLM-L6-v2
MEMORY_ENABLED=true
MEMORY_MIN_IMPORTANCE=0.60
LLM_CACHE_ENABLED=true
MAX_LOCAL_LLM_CONCURRENCY=2
MAX_CLOUD_LLM_CONCURRENCY=1
MAX_LOCAL_LLM_CALLS_PER_SCAN=100
MAX_CLOUD_LLM_CALLS_PER_SCAN=5
MAX_CLOUD_LLM_CALLS_PER_MINUTE=5
MAX_RAG_QUERIES_PER_SCAN=50
DATABASE_URL=postgresql+psycopg2://swarmshield:swarmshield@localhost:5432/swarmshield

# --- GitHub (optional; powers the auto-PR remediation workflow) ---
# Leave blank to run with the auto-PR feature disabled.
GITHUB_TOKEN=
GITHUB_REPO=owner/repo
GITHUB_BASE_BRANCH=main
